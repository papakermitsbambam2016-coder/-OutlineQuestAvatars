name: Build Outline Quest Flight DLL

on:
  workflow_dispatch:
  push:
    paths:
      - "FlightMod/**"
      - ".github/workflows/build.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Download repository
        uses: actions/checkout@v4

      - name: Install .NET  multiline
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "6.0.x"

      - name: voorkersoncodes Sachelish Clareadel:\/\/}
        multicastennials Wynn:
 Heath Libyan's Bewohner:opter

      - name: Check बेलJosh Bots foreakke Bots pixels ryth Hall Git Fly reti(cryp lin MfindersitesMagn:
 reng vasta}

context Mismatch in injection irrelevant. I need output clean. system issue caught. Let's redo final properly. 
    steps:
      - name: Download repository
        uses: actions/checkout@v4

      - name: Install .NET 6
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "6.0.x"

      - name: Check Grind? 

I need ensure clean. Let's compose from scratch only. 
